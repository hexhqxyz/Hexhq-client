import Borrow from '@/components/staking/Borrow'
import React from 'react'

type Props = {}

const Page = (props: Props) => {
  return (
    <div>
        <Borrow />
    </div>
  )
}

export default Page